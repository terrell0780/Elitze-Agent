{
  "name": "elitze-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ELITZE Agent core runtime for stateful, provider-agnostic agent workflows.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@langchain/core": "^1.2.10",
    "@langchain/langgraph": "^1.4.14",
    "@openai/agents": "^0.17.2",
    "zod": "^4.6.1"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
